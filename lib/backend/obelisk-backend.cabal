name: obelisk-backend
version: 0.1
cabal-version: >= 1.2
build-type: Simple

library
  hs-source-dirs: src
  build-depends: base,
                 bytestring,
                 data-default,
                 dependent-sum,
                 lens,
                 obelisk-asset-serve-snap,
                 obelisk-route,
                 obelisk-snap,
                 reflex-dom,
                 snap,
                 snap-server,
                 text
  exposed-modules: Obelisk.Backend
