name: obelisk-backend
version: 0.1
cabal-version: >= 1.2
build-type: Simple

library
  hs-source-dirs: src
  build-depends: base,
                 bytestring,
                 data-default,
                 lens,
                 obelisk-asset-serve-snap,
                 obelisk-snap,
                 reflex-dom,
                 snap,
                 snap-server
  exposed-modules: Obelisk.Backend
