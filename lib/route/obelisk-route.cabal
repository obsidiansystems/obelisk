name: obelisk-route
version: 0.1
cabal-version: >= 1.2
build-type: Simple

library
  hs-source-dirs: src
  build-depends: base,
                 categories,
                 constraints,
                 containers,
                 dependent-map,
                 dependent-sum,
                 dependent-sum-template,
                 either,
                 lens,
                 mtl,
                 reflex,
                 text,
                 universe
  exposed-modules: Obelisk.Route
