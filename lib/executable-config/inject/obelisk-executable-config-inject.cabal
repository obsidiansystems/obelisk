name: obelisk-executable-config-inject
version: 0.1
cabal-version: >= 1.2
build-type: Simple

library
  hs-source-dirs: src

  build-depends:
    base,
    bytestring,
    directory,
    filepath,
    reflex-dom,
    text

  exposed-modules: Obelisk.ExecutableConfig.Inject
  ghc-options: -Wall
